# Copy this file to .env and fill values for real model calls.
# Keep .env out of git. It is already listed in .gitignore.
# mock runs locally without an API key. Use api for real LLM calls.
SAGAP_LLM_PROVIDER=mock
# Required only when SAGAP_LLM_PROVIDER=api.
SAGAP_LLM_API_KEY=
# Choose the model available to your API account.
SAGAP_LLM_MODEL=gpt-4.1-mini
# Required only when SAGAP_LLM_PROVIDER=api.
# Use the root URL for a Chat Completions compatible endpoint, for example: https://example.com/v1
SAGAP_LLM_BASE_URL=
# Default runtime settings.
SAGAP_OUTPUT_DIR=outputs
SAGAP_TEMPERATURE=0.2
SAGAP_MAX_OUTPUT_TOKENS=4096
